# Homebrew Tap

Homebrew tap for `alexcamposruiz/acr-cli`.

```sh
brew tap alexcamposruiz/tap
brew install acr-cli
```

Formula updates are published by GoReleaser from tagged releases in `github.com/alexcamposruiz/acr-cli`.
